[mysqld]
# Listen on all IPv4 + IPv6 addresses. Required so the backend can reach this
# machine over Fly's private IPv6 network (doctor-appointment-mysql.internal).
bind-address = *
mysqlx = OFF
# Modest footprint for a small Fly machine.
innodb_buffer_pool_size = 256M
